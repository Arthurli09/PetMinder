using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class DisplayPetExpData : MonoBehaviour
{
    /*public GameObject hBar;
    // Start is called before the first frame update
    void Start()
    {
         GetComponent<TextMeshProUGUI>().text = "Level: " + bar.GetComponent<FillBar>().lvl.ToString() + " " + bar.GetComponent<FillBar>().curExp.ToString() + "/" + hBar.GetComponent<FillBar>()..ToString();
    }

    // Update is called once per frame
    void Update()
    {
         GetComponent<TextMeshProUGUI>().text = "Level: " + bar.GetComponent<FillBar>().lvl.ToString() + " " + bar.GetComponent<FillBar>().curExp.ToString() + "/" + bar.GetComponent<FillBar>().totExp.ToString();
    }*/
}
